<%= form_for(current_user.watchlists.find_by_item_id(@item),
             html: { method: :delete }, remote: true) do |f| %>
    <%= f.submit "Remove from Watchlist", class: "btn btn-small" %>
<% end %>